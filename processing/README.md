
# Processing

This folder contains the logic used to convert raw match JSON files into a structured ball-by-ball dataset.

## File
- `json_to_df.py`  
  Reads multiple raw JSON files, extracts nested match and commentary fields, and combines them into a single analysis-ready CSV.
